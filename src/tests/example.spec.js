describe('Breeds list', () => {
    it('should show a list of breeds', () => {
    });
});

describe('Dogs', () => {
    it('should display 3 dogs', () => {
    });

    it('should display an error when no dogs exist', () => {
    });
});
